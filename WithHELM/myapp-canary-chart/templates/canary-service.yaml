apiVersion: v1
kind: Service
metadata:
  name: myapp-canary-service
spec:
  selector:
    app: myapp
    version: canary
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 80
  type: ClusterIP
