apiVersion: v1
kind: Service
metadata:
  name: myapp-stable-service
spec:
  selector:
    app: myapp
    version: stable
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 80
  type: ClusterIP
